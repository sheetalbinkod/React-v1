export const colors = {
  black: (alpha = 1) => `rgba(0, 0, 0, ${alpha})`, // #000
  white: (alpha = 1) => `rgba(255, 255, 255, ${alpha})`, // #fff
  gold: (alpha = 1) => `rgba(256, 183, 43, ${alpha})`, // #fdb72b
  teal: (alpha = 1) => `rgba(46, 172, 155, ${alpha})`, // #2eac9b
  lightBlue: (alpha = 1) => `rgba(30, 138, 191, ${alpha})`, // #1e8abf
  darkBlue: (alpha = 1) => `rgba(14, 89, 124, ${alpha})`, // #0e597c
  slate: (alpha = 1) => `rgba(58, 65, 80, ${alpha})`, // #3a4150'
  gray3: '#333',
  gray4: '#444',
  gray6: '#666',
  gray9: '#999',
  grayc: '#ccc',
  graye: '#eee',
};

export const fonts = {
  primary: 'Montserrat, Georgia, Arial, serif',
  secondary: '"Source Sans Pro", sans-serif',
};
